dist: xenial   # required for Python >= 3.7
language: python
python:
 - "3.7"
install: 
 pip3 install --upgrade pip 
script: 
 pytest -s -q
after_script:
 - sudo chown -R travis ./travis/push.sh
 - sudo chmod +x ./travis/push.sh
after_success:
 - ./travis/push.sh
